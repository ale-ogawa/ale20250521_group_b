<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.webapp.repository.UserMapper">
<select id="getUserSet" resultType="com.example.webapp.entity.User">

SELECT d.name AS diseaseName
FROM choices c
LEFT JOIN diseases d ON c.disease_id = d.id
WHERE c.account_id = #{id};

</select>

</mapper>