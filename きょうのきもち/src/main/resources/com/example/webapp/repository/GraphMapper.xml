<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.webapp.repository.GraphMapper">
  <select id="getUserdiaries" resultType="com.example.webapp.entity.Graph">
    SELECT date, feeling FROM diaries ORDER BY date;
  </select>
</mapper>